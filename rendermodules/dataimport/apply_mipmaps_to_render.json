{
    "render": {
        "host":"{{render_host}}",
        "port": "{{render_port}}",
        "owner": "{{render_owner}}",
        "project": "{{render_project}}",
        "client_scripts": "{{render_client_scripts}}"
    },
    "input_stack": "{{input_stack}}",
    "output_stack": "{{output_stack}}",
    "mipmap_dir": "{{mip_map_dir}}",
    "imgformat": "tif",
    "levels": 6,
    "zstart": {{zstart}},
    "zend": {{zend}}
}